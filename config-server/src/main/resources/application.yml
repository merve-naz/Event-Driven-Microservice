server:
  port: 8888

spring:
  application:
    name: configserver

  cloud:
    config:
      server:
        git:
          uri: file:///C:/Users/merve/Desktop/microservices-demo/config-server-repository
          clone-on-start: true

    fail-fast: true